spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: test-data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
